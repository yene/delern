{
  "database": {
    "rules": "database.rules.bolt"
  },
  "functions": {
    "source": "functions",
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR\" run check",
      "npm --prefix \"$RESOURCE_DIR\" run compile"
    ]
  }
}
